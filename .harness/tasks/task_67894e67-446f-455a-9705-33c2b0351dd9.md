Model: openai-codex/gpt-5.5
Reasoning Level: low

# Center Git Stack Browser Staged and Unstaged Expand Icons

## 1. Requirements & Constraints

- **REQ-001**: The `+` and `-` expand/collapse indicators for `staged` and `unstaged` items in the stack browser git menu must be horizontally centered over the vertical child guide line.
- **REQ-002**: The fix must preserve existing expand/collapse behavior for git tree nodes.
- **REQ-003**: The fix must not change unrelated stack browser layout, file labels, selection behavior, or keyboard interaction.
- **REQ-004**: Add or update focused regression coverage when the relevant rendering surface has existing tests.
- **CON-001**: Planning-only mode is active; do not edit files during planning.
- **CON-002**: Exact source ownership is unknown from provided context; implementation must begin with repository discovery.
- **CON-003**: Use `pnpm build` as the required final validation command.
- **GUD-001**: Prefer the smallest CSS/layout change that aligns the icon center with the existing vertical child guide line.
- **PAT-001**: Follow existing stack browser styling conventions after discovering the relevant component and stylesheet.

## 2. Implementation Steps

### Implementation Phase 1

- **GOAL-001**: Locate the stack browser git menu implementation and identify the layout rule causing the icon offset.

| Task | Description | Dependencies | Acceptance Criteria |
| ---- | ----------- | ------------ | ------------------- |
| TASK-001 | Inspect repository structure from `c:\dev\jasonshell` and locate stack browser source files using searches for strings including `staged`, `unstaged`, `stack browser`, `StackBrowser`, `git menu`, and expand/collapse icon renderers. | None | A concrete list of candidate source files and test files is identified. |
| TASK-002 | Inspect relevant documentation files: `docs/stack-browser-improvement-audit.md`, `docs/stack-browser-phase-1-stabilize-safety-plan.md`, and `docs/remediation-plans/04-stack-paging-resource-bounds.md` for stack browser architecture and styling constraints. | None | Any applicable architectural or styling constraints are recorded before implementation. |
| TASK-003 | Identify the exact component/template that renders the `staged` and `unstaged` git tree group rows and the stylesheet or layout code that draws the vertical child guide line. | TASK-001, TASK-002 | The implementer can name the exact files, selectors/classes/components, and layout properties responsible for both the icon and vertical guide line. |

### Implementation Phase 2

- **GOAL-002**: Add a regression test or visual/layout assertion for expand icon alignment where practical.

| Task | Description | Dependencies | Acceptance Criteria |
| ---- | ----------- | ------------ | ------------------- |
| TASK-004 | Discover existing tests for stack browser rendering, git menu rendering, tree row indentation, or CSS/layout snapshots. | TASK-003 | Existing test approach is identified, or lack of suitable tests is documented. |
| TASK-005 | If component/layout tests exist, add a failing regression test that renders `staged` and `unstaged` rows with children and verifies the expand icon center aligns with the child vertical guide line x-coordinate. | TASK-004 | Test fails before the layout fix and passes after the layout fix. |
| TASK-006 | If no practical automated layout test exists, add the closest available regression coverage, such as a DOM class/structure test ensuring both icon and guide line share the same alignment container or CSS variable. | TASK-004 | Test fails before the layout fix where possible, or documents why only structural coverage is feasible. |

### Implementation Phase 3

- **GOAL-003**: Apply the minimal layout/style change to center the `+` and `-` indicators over the child guide line.

| Task | Description | Dependencies | Acceptance Criteria |
| ---- | ----------- | ------------ | ------------------- |
| TASK-007 | Modify the discovered stack browser git tree row styles so the expand/collapse indicator for parent rows uses the same horizontal centerline as the vertical child guide line. Prefer shared CSS variables/constants for indentation and guide offset if present. | TASK-003 | In the UI, the center of `+` and `-` for `staged` and `unstaged` is directly above the vertical child guide line. |
| TASK-008 | Verify the same alignment applies to both expanded and collapsed states for `staged` and `unstaged`. | TASK-007 | `+` and `-` have identical horizontal center positions relative to the guide line. |
| TASK-009 | Verify the change does not shift file names, child file rows, selection highlight bounds, or other stack browser tree groups unexpectedly. | TASK-007 | Only the expand/collapse indicator alignment changes, unless a shared tree alignment correction is intentionally required and documented. |

### Implementation Phase 4

- **GOAL-004**: Validate build, tests, and manual behavior.

| Task | Description | Dependencies | Acceptance Criteria |
| ---- | ----------- | ------------ | ------------------- |
| TASK-010 | Run focused tests discovered or added for stack browser/git menu rendering. | TASK-005 or TASK-006 or TASK-007 | Focused tests pass. |
| TASK-011 | Run `pnpm build` from `c:\dev\jasonshell`. | TASK-010 | Build exits successfully with code `0`. |
| TASK-012 | Perform a manual smoke check in the application UI, opening the stack browser git menu with both staged and unstaged files present. | TASK-011 | The `+`/`-` indicators for `staged` and `unstaged` are visually centered over the vertical guide line and expand/collapse remains functional. |
| TASK-013 | Update `changelog.md` according to `CHANGELOG_POLICY.md` if project policy requires a changelog entry for UI behavior/style changes. | TASK-007 | Changelog either includes a concise entry or implementer documents that policy does not require one. |

## 3. Alternatives

- **ALT-001**: Shift the vertical guide line left to match the current icon position. Rejected because the requested outcome is to center the icons over the existing line, and moving the line may disturb child file alignment.
- **ALT-002**: Replace text `+` and `-` with SVG icons. Rejected because the issue is alignment, not icon design, and this would create unnecessary scope.
- **ALT-003**: Apply a hardcoded pixel offset only to `staged` and `unstaged`. Rejected unless no shared tree style exists, because a shared alignment variable is less brittle.

## 4. Dependencies

- **DEP-001**: Existing stack browser UI component that renders git menu tree/group rows.
- **DEP-002**: Existing stylesheet, CSS module, terminal UI renderer, or layout constants controlling indentation and guide lines.
- **DEP-003**: Existing test framework in the repository, discoverable from `package.json` scripts and test directories.
- **DEP-004**: Package manager command `pnpm build`.

## 5. Files

- **FILE-001**: `docs/stack-browser-improvement-audit.md` — inspect for stack browser architecture and known layout issues.
- **FILE-002**: `docs/stack-browser-phase-1-stabilize-safety-plan.md` — inspect for stack browser stabilization constraints.
- **FILE-003**: `docs/remediation-plans/04-stack-paging-resource-bounds.md` — inspect for stack browser implementation context.
- **FILE-004**: `CHANGELOG_POLICY.md` — inspect before deciding whether to update changelog.
- **FILE-005**: `changelog.md` — update only if policy requires a UI alignment fix entry.
- **FILE-006**: Discovery target: stack browser git menu component file located by searching for `staged`, `unstaged`, and stack browser renderer names.
- **FILE-007**: Discovery target: stack browser stylesheet/layout constants file controlling tree row indentation and vertical child guide line.
- **FILE-008**: Discovery target: corresponding stack browser/git menu test file, if one exists.

## 6. Testing

- **TEST-001**: Add or update a focused regression test for `staged` and `unstaged` group row expand icon alignment when a suitable UI/layout test harness exists.
- **TEST-002**: Run the focused stack browser/git menu test command discovered from repository scripts.
- **TEST-003**: Run `pnpm build`.
- **TEST-004**: Manual smoke check: open stack browser git menu with staged and unstaged child files, toggle both groups, and verify `+`/`-` centers align with the vertical child guide line.

## 7. Risks & Assumptions

- **RISK-001**: CSS/layout may be shared with other tree views. Mitigation: inspect all consumers of the changed selector/constant and manually verify no unintended row shifts.
- **RISK-002**: Pixel-perfect visual alignment may not be covered by automated tests. Mitigation: add structural regression coverage and require manual smoke verification.
- **RISK-003**: Text glyph metrics for `+` and `-` may differ by font. Mitigation: center the icon container, not the glyph using ad hoc padding.
- **ASSUMPTION-001**: The stack browser git menu is implemented in local source files rather than generated or external package code. Verify during TASK-001.
- **ASSUMPTION-002**: `pnpm build` is the authoritative build validation command. Verify using `package.json` before running.

## 8. Related Specifications / Further Reading

- **REF-001**: `master_spec.md` — canonical project architecture and behavior specification.
- **REF-002**: `CHANGELOG_POLICY.md` — rules for changelog updates.
- **REF-003**: `docs/stack-browser-improvement-audit.md` — stack browser audit context.
- **REF-004**: `docs/stack-browser-phase-1-stabilize-safety-plan.md` — stack browser stabilization plan.
- **REF-005**: `docs/remediation-plans/04-stack-paging-resource-bounds.md` — stack browser remediation context.

---
The plan file is located at c:\dev\jasonshell\.harness\tasks\task_67894e67-446f-455a-9705-33c2b0351dd9.md
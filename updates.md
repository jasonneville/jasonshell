* Ensure that when clicking on a icon in the tray that it does not get rid of the tray view underneath. 
* Ensure the quick commands view looks the same as the theme and matches the rest of the GUI. 
* Ensure I can start the drag selection from all the way to the end of the margin of the stack browser, instead of having to have a very small space inside the stack browser allow it to go all the way to the outside. 
* Ensure that when launching some icons from the quick launch icons that they does not disappear after there there's an issue. I'm having this issue with terminal and Spotify applications. Ensure that it launches them and does not fail because of permissions or anything else. 
* Ensure that the task previews have a red X in the top right of the preview in order to allow for easy closing. Also, allow the mouse to be anywhere inside of the task preview without it closing. Currently, if it's in the bottom half, it is fine, but once the mouse goes into the top half of the task preview, it automatically disappears. 
* Ensure in the sound settings that it automatically updates on in input or output. Currently it is waiting until we press the refresh button, but it should automatically refresh. 
* Add a Clear button to the search on the top right as well as inside the search middle view to automatically make the search empty without having to select all and backspace. 
* Add the ability to change positions and drag left or right on the top bar folders, just like you can with the open processes on the bottom bar. 
* Add autocomplete to the stack browser path when typing in there to make it easier to traverse between directories. 
* Update the stack browser to not freeze when we are loading a large file and the icons are still loading. Currently until the icons are fully loaded, it is freezing the display when it should happen in the background to allow the display not to freeze. 
* Ensure that the reveal option when inside a folder in the Stack browser that it is always populated and able to open in Windows Explorer. For some reason there are a lot of folders where I don't have the option selectable and cannot open it in Windows Explorer. 
* Add the ability to extract zip files and RAR files using Windows or 7-zip for ease of use in the stack browser rather than having to go to the Windows Explorer to do such operations. 
* Add more intelligent context menu actions when right-clicking in the Stack Browser including properties, etc. 
* Add the ability to right-click an icon and the quick launch icons to unpin from the taskbar as well. Currently we can only pin to it. 
* Add the ability for the search to get triggered when using the Windows key rather than using the Windows default search. Override this until the application is closed. So while the application is active, when we press the Windows key it always launches our search.  

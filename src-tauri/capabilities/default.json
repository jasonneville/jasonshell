{
  "identifier": "top-bar",
  "description": "Top-bar shell surface capability: search, workspace activation, stack pin, workspace tooling action plans, event, and window-label access.",
  "windows": [
    "top-bar"
  ],
  "permissions": [
    "core:default",
    "core:window:default"
  ]
}

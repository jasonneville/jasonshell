# P04 Windows save/recovery feasibility

Status: **BLOCKED — constrained repair fails closed** (2026-09-16). Packet 08 verifies only this test-only recovery-directory ACL property: current-user ownership and full-control directory ACE are required; explicit allows are limited to current user and SYSTEM; inherited allows are limited to current user, SYSTEM, and Builtin Administrators; accepted allows must use the documented full-control directory rights/flags. Any other allow principal/right fails. This is not a generic “private DACL” claim. Win32 ACL errors and malformed/NUL/overwide paths emit bounded generic diagnostics without recovery path, key, or content. Native `ReplaceFileW` metadata observation passes. Process-child termination before and after publication reopens durable journals as `staged` and `publication-possible`; simulated quota/disk-full and retention limiting preserves dirty assets. This host still refuses required oplock (`os=300`), so native in-place save remains unsupported here; Save As create-new remains available. No generic race-free `ReplaceFileW` claim exists.

Implemented evidence path: same-directory retained stage plus distinct publication candidate, full writes and `sync_all`, source-handle oplock acquisition, then handle-based file-id/length/mtime/link-count/SHA-256 revalidation and a second intact check immediately before `ReplaceFileW`. Hashing streams in 64 KiB blocks. Oplock refusal/break or identity/content mismatch fails closed and retains staged ours; standard sharing/byte locks are not treated as protection against old writable mappings. The real `replace()` API returns typed `SaveError`; actual-caller injection after successful `ReplaceFileW` verifies `PublicationPossible`, ambiguous restart classification, preserved target/staging/backup paths, retained files, and exactly one publication with no retry.

Recovery decision: experiment-only schema version 1 stores immutable base, ordered durable edit pages, and committed manifest exclusively in existing AES-256-GCM `PageStore` pages with a DPAPI-protected per-store key. Reopen/reconstruction, plaintext absence, authenticated corruption refusal, quarantine classification, exact allowlist ACL validation, process-child termination, bounded redacted diagnostics, and quota-failure asset retention pass under synthetic roots carrying the ACL property above. Disk-full is explicitly simulated; real OS disk-full and full age/global retention remain unproved. Therefore `snapshotComplete` remains distinct from `recoveryComplete`; no plaintext recovery journal was added.

See newest packet `test-results/stack-text-editor/P04/20260916-p04-save-recovery-08/`. Final focused result is recorded there. Native successful oplock publication, forced crash, complete retention policy, real disk-full attribution, and named reviews remain blocked.
